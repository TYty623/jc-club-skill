---
name: jc-club-skill
description: 设计或演进 Spring Cloud 社区平台的网关、认证、题库与练习等服务边界。适用于需要保留清晰领域分层、统一登录上下文和服务治理的 Java 微服务。
---

# Community Platform Service Boundaries

用清晰的服务职责和领域层次扩展社区平台，避免将网关、鉴权、业务规则、RPC 和基础设施逻辑耦合在同一模块。

## 适用范围

- 程序员社区、题库、练习、面试或内容互动等多模块 Java 服务。
- Spring Cloud 网关后接多个业务服务，并需要统一认证和请求上下文。
- 业务模块要在 API、应用、领域与基础设施之间分层。

不要为单体小工具引入服务拆分；只有独立部署、自治团队或明确的负载/安全边界能带来价值时才拆分。

## 服务边界

1. 网关：路由、跨域、粗粒度访问控制、请求 ID 与统一错误转换；不放领域规则。
2. 认证服务：签发和验证身份凭据，提供可信的用户/角色上下文；业务服务不相信未经验证的客户端头。
3. 领域服务：拥有自己的业务规则和数据模型。跨域调用使用稳定的契约，避免直接依赖对方的持久化实现。
4. 应用层：编排用例、事务边界和事件；领域层保持无框架或低框架依赖。
5. 基础设施层：数据库、缓存、RPC、消息与第三方 SDK 的适配；接口由内层定义。

## 实施顺序

1. 先为每项新能力写出拥有它的服务、输入/输出契约、身份需求和失败语义。
2. 在网关生成请求 ID；在认证边界验证身份；只将已验证的声明传入业务用例。
3. 将查询、命令和异步任务的幂等要求写入契约，尤其是点赞、练习提交和通知类操作。
4. 为缓存、RPC 和消息设置超时、降级和可观测性；不要把网络失败伪装成“无数据”。
5. 用契约测试验证服务间行为；用领域测试验证业务规则，而不是只测控制器返回码。

## 复用资料

- 阅读 [架构提炼](references/architecture.md) 了解职责分割。
- 阅读 [模块地图](references/module-map.md) 选择适合放置新能力的模块。
- [RequestScope.java](examples/java/RequestScope.java) 展示如何保存已验证的请求上下文；验证本身必须由认证边界完成。

## 边界说明

本仓库只包含对本地社区项目的原创总结与独立示例，不包含原项目源码、配置、部署资产或课程材料。
